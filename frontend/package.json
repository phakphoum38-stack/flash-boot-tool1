{
  "main": "electron/main.js",
  "scripts": {
    "build": "vite build",
    "dist": "npm run build && electron-builder"
  },
  "build": {
    "files": [
      "dist/**/*",
      "electron/**/*",
      "../backend/dist/main.exe"
    ],
    "win": {
      "target": "nsis"
    }
  }
}
